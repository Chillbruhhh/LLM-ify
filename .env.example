# Copy this file to .env and add your API keys (LLM-ify also reads config.json when present)

# OpenAI API Key
# Get your API key from https://platform.openai.com
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI model override (optional)
# OPENAI_MODEL=gpt-4.1-nano

# OpenAI base URL override (optional, for OpenRouter use https://openrouter.ai/api/v1)
# OPENAI_BASE_URL=

# Provider selection (openai or openrouter)
# OPENAI_PROVIDER=openai

# OpenRouter API Key (optional)
# If set and OPENAI_API_KEY is empty, this will be used automatically.
# OPENROUTER_API_KEY=your-openrouter-api-key-here

# Maximum concurrent crawlers for Crawl4AI (default: 10)
# Adjust based on your system resources (recommended: 5-20)
MAX_CONCURRENT_CRAWLERS=10

# Crawl4AI seeding defaults (optional) ~ config.json will save your prefences by default after first run!
# SEED_SOURCE=sitemap+cc
# SEED_QUERY=
# SEED_SCORING_METHOD=
# SEED_SCORE_THRESHOLD=
# SEED_LIVE_CHECK=false
# SEED_EXTRACT_HEAD=true
# SEED_FILTER_NONSENSE=true
# SEED_INCLUDE_PATTERNS=
# SEED_EXCLUDE_PATTERNS=
